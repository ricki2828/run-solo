name: run_engine
description: Pure-Dart analysis engine for Run Solo (laps, trimming, metrics, staged verdict, plan schedule). No Flutter, no I/O.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  xml: ^6.5.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
  coverage: ^1.15.0
