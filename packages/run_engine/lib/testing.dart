/// Test-only support for Run Solo: the synthetic trace generator and its
/// fixture specs. Kept out of `package:run_engine/run_engine.dart` so the
/// app never ships it.
library;

export 'src/synthetic/trace_generator.dart';
