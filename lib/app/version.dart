/// The running app version, kept equal to `pubspec.yaml` `version:` by
/// `test/app_version_test.dart` (no package_info dependency). The Lap Draw
/// intro plays in full once per version (plan §4, D8).
const String kAppVersion = '0.1.0+1';
